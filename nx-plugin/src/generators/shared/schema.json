{
  "$schema": "http://json-schema.org/draft-07/schema",
  "id": "shared",
  "type": "object",
  "cli": "nx",
  "properties": {
    "name": {
      "type": "string",
      "description": "Shared library's name",
      "x-prompt": "What is the library's name?"
    },
    "skipComponent": {
      "type": "boolean",
      "description": "Don't generate a component."
    }
  },
  "required": [
    "name"
  ]
}
